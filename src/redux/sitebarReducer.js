let initialState = {
    friends: [
        {id: 1, name: 'Ilya'},
        {id: 2, name: 'Lilya'},
        {id: 3, name: 'Misha'}
    ]
}

const sitebarReducer = (state = initialState, action) => {
    return state;
}

export default sitebarReducer;